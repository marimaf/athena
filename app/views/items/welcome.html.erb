<h1>WELCOME USER!!!!</h1>

<% if !user_signed_in? %>
	<%= link_to('Register', new_user_session_path)  %>
	<%end%> 

<table>
  <tr>
    <th></th>
  </tr>

<% @items.each do |item| %>
  <tr>
    <td><%= link_to item.name + " (" + number_to_currency(item.price) + ")" , item %></td>
    
  </tr>
<% end %>
</table>
